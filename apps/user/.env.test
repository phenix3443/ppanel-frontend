# 【必须提交进仓库】测试跑在 vite 的 test 模式下，只会读 .env / .env.test。
# 原来测试是靠各人本机的 .env.local 才能跑起来——那个文件在 .gitignore 里，
# 于是 CI 上必挂（packages/ui 的 request.ts 在模块加载时就校验这两个变量）。
# 值本身无所谓，测试不发真实请求，只要不是空的就行。
VITE_API_BASE_URL=http://localhost:8080
VITE_API_PREFIX=
